services:
  - type: web
    name: ki-status-app
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: DELETE_PASSWORD
        sync: false
